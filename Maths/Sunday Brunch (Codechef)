#include <iostream>

int main() {
 
    int t, x, y;
    scanf("%d", &t);
    
    while(t--){
        scanf("%d %d", &x, &y);
        
        if(x/y>=20) printf("%d\n", 20);
        else printf("%d\n", x/y);
    }
	return 0;
}
