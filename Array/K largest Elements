#include <bits/stdc++.h> 
vector<int> Klargest(vector<int> &a, int k, int n) {
    // Write your code here

    sort(a.begin(), a.end());
    vector<int> ans;

int i = n - k;

    while(i<n){
        ans.push_back(a[i++]);
    }
 
return ans;
}
